# Skyie Forge — On-demand pod runtime dependencies.
#
# This file lives on the network volume at /runpod-volume/forge-app/.
# The first pod to boot on a fresh volume creates a venv and pip-installs
# this list (~3 minutes). Every subsequent pod reuses the same venv.
#
# We do NOT include CUDA/PyTorch here — those come from the runpod/pytorch
# base image. We pin the inference + utility libraries on top.

# FastAPI shim (serve.py runs uvicorn) + outbound HTTP for self-registration
fastapi==0.115.5
uvicorn[standard]==0.32.0
httpx==0.27.2

# Diffusers stack (FLUX support landed in 0.30; pin known-good)
diffusers==0.32.2
transformers==4.46.3
accelerate==1.1.1
safetensors==0.4.5
peft==0.13.2                # LoRA loading via PEFT adapter
huggingface_hub==0.26.5
sentencepiece==0.2.0        # T5 tokeniser (FLUX text encoder 2)
protobuf>=4.25,<6
hf_transfer                 # parallel downloads (HF_HUB_ENABLE_HF_TRANSFER=1)

# PuLID for FLUX — identity preservation from one reference photo
einops==0.8.0
timm==1.0.11

# Face detection + embedding (PuLID dependency)
insightface==0.7.3
onnxruntime-gpu==1.20.1
facexlib==0.3.0
filterpy==1.4.5             # facexlib transitive
opencv-python-headless==4.10.0.84

# Image I/O + utility
Pillow==11.0.0
requests==2.32.3
numpy<2                     # diffusers pinned to numpy<2

# CLIP for PuLID (EVA-CLIP variant)
open_clip_torch==2.29.0
